///////////////////////////////////////////////////////////
/// @brief NEW_CLASS_NAME
///
class NEW_CLASS_NAME
{
public:
    NEW_CLASS_NAME();
    virtual ~NEW_CLASS_NAME(); // TODO: Is VIRTUAL needed?
    NEW_CLASS_NAME(const NEW_CLASS_NAME &other);
    NEW_CLASS_NAME(NEW_CLASS_NAME &&other);
    NEW_CLASS_NAME &operator=(const NEW_CLASS_NAME &other);
    NEW_CLASS_NAME &operator=(NEW_CLASS_NAME &&other);

private:
    // Members
};